name: Build & Deploy (Terraform)

on:
  push:
    tags:
      - 'v*'
      - 'V*'
      - '!*-alpha*'
      - '!*-Alpha*'
  workflow_dispatch:
    inputs:
      image_tag:
        description: '手动指定镜像 tag（留空则自动生成）'
        required: false
        default: ''

permissions:
  contents: read
  id-token: write

env:
  TCR_REGISTRY: xiongfei-test.tencentcloudcr.com
  TCR_NAMESPACE: burton-namespace
  TF_WORKING_DIR: infra/terraform

jobs:
  # ========================================================
  # Stage 1: Build & Push Docker Image to TCR
  # ========================================================
  build:
    name: Build & Push Image
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.value }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Determine version
        id: version
        run: |
          # 优先使用手动输入的 tag
          if [[ -n "${{ inputs.image_tag }}" ]]; then
            VERSION="${{ inputs.image_tag }}"
          elif [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          else
            VERSION="manual-$(date +'%Y%m%d')-$(git rev-parse --short HEAD)"
          fi
          echo "$VERSION" > VERSION
          echo "value=$VERSION" >> "$GITHUB_OUTPUT"
          echo "::notice::Image version: $VERSION"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to TCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.TCR_REGISTRY }}
          username: ${{ secrets.TCR_USERNAME }}
          password: ${{ secrets.TCR_PASSWORD }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.TCR_REGISTRY }}/${{ env.TCR_NAMESPACE }}/new-api:${{ steps.version.outputs.value }}
            ${{ env.TCR_REGISTRY }}/${{ env.TCR_NAMESPACE }}/new-api:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ========================================================
  # Stage 2: Terraform Apply — 声明式更新 K8s Deployment
  # ========================================================
  deploy:
    name: Terraform Deploy
    needs: build
    runs-on: ubuntu-latest
    environment: production   # 可在 GitHub Settings 设置审批保护
    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR }}
    env:
      # Terraform 通过环境变量读取腾讯云凭证
      TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENTCLOUD_SECRET_ID }}
      TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENTCLOUD_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9"

      - name: Terraform Init
        run: |
          terraform init -input=false \
            -backend-config="secret_id=${{ secrets.TENCENTCLOUD_SECRET_ID }}" \
            -backend-config="secret_key=${{ secrets.TENCENTCLOUD_SECRET_KEY }}"

      - name: Terraform Apply - Phase 1 (Tencent Cloud infrastructure only)
        run: |
          # 第一阶段：只创建腾讯云基础资源，不包括 K8s Provider 管理的资源
          # 因为 K8s Provider 在集群创建之前无法配置
          terraform apply -auto-approve -input=false \
            -var="app_image_tag=${{ needs.build.outputs.version }}" \
            -var="secret_id=${{ secrets.TENCENTCLOUD_SECRET_ID }}" \
            -var="secret_key=${{ secrets.TENCENTCLOUD_SECRET_KEY }}" \
            -var="tcr_namespace=${{ secrets.TCR_NAMESPACE }}" \
            -var="db_password=${{ secrets.DB_PASSWORD }}" \
            -var="redis_password=${{ secrets.REDIS_PASSWORD }}" \
            -var="session_secret=${{ secrets.SESSION_SECRET }}" \
            -target=tencentcloud_vpc.main \
            -target=tencentcloud_subnet.app \
            -target=tencentcloud_subnet.db \
            -target=tencentcloud_security_group.app \
            -target=tencentcloud_security_group.db \
            -target=tencentcloud_security_group_rule_set.app_rules \
            -target=tencentcloud_security_group_rule_set.db_rules \
            -target=tencentcloud_postgresql_instance.main \
            -target=tencentcloud_redis_instance.main \
            -target=tencentcloud_kubernetes_cluster.main \
            -target=tencentcloud_kubernetes_serverless_node_pool.app \
            -target=tencentcloud_kubernetes_cluster_endpoint.main

      - name: Terraform Apply - Phase 2 (K8s resources)
        run: |
          # 第二阶段：在集群就绪后部署 K8s 资源（Namespace, Secret, Deployment, Service）
          terraform apply -auto-approve -input=false \
            -var="app_image_tag=${{ needs.build.outputs.version }}" \
            -var="secret_id=${{ secrets.TENCENTCLOUD_SECRET_ID }}" \
            -var="secret_key=${{ secrets.TENCENTCLOUD_SECRET_KEY }}" \
            -var="tcr_namespace=${{ secrets.TCR_NAMESPACE }}" \
            -var="db_password=${{ secrets.DB_PASSWORD }}" \
            -var="redis_password=${{ secrets.REDIS_PASSWORD }}" \
            -var="session_secret=${{ secrets.SESSION_SECRET }}"

      - name: Verify deployment
        run: |
          APP_URL=$(terraform output -raw app_url 2>/dev/null || echo "")
          IMAGE=$(terraform output -raw app_image 2>/dev/null || echo "")

          echo "### 🚀 Deploy Complete" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| **Version** | \`${{ needs.build.outputs.version }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| **Image** | \`${IMAGE}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| **URL** | ${APP_URL} |" >> "$GITHUB_STEP_SUMMARY"
